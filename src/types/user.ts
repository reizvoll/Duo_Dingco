export interface User {
    id: string;
    img_url?: string;
    nickname: string;
  }

export interface UserData {
    id: string;
    img_url?: string;
    nickname: string;
    created_at?: string;
    Exp?: number
    Lv?: number
  }